<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:windowBackground">@color/appBackgroundColor</item>
        <item name="android:textColor">@color/appTextColor</item>

        <!-- Other customizations, such as buttons, EditText, etc. -->
        <item name="android:editTextColor">@color/appTextColor</item>
        <item name="android:textColorPrimary">@color/appTextColor</item>
    </style>
</resources>
